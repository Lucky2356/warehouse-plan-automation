**Кнопка «Открыть в Excel»** в карточке результата — у обеих задач. Открывает готовую
книгу сразу, без похода в проводник. Кнопка «Папка» осталась рядом.

**Темы стали по-настоящему разными.** В светлой теме боковая панель теперь тоже светлая:
выбор между темами настоящий, а не «тёмная и тёмная с белым содержимым». Обе палитры
чуть контрастнее, шрифт плотнее и немного крупнее — окно читается весь день.

**Сохранённая тема применялась не с первого раза.** В 1.10.0 выбранная тёмная тема
при следующем запуске не вставала: окно открывалось светлым. Причина в том, как
подменялся словарь цветов. Исправлено.

---

Скачайте единственный `.exe` и запустите — установка и распаковка не нужны.

Требования: Windows x64 и установленный Microsoft Excel (2016 и новее, включая 2021 и 365).
Отдельная установка .NET не нужна: всё встроено в файл.
